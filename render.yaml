services:
  - type: web
    name: cirkle-commission-bot
    env: node
    region: frankfurt
    plan: free
    buildCommand: npm install --prefix discord-bot
    startCommand: npm start --prefix discord-bot
    envVars:
      - key: NODE_ENV
        value: production
      - key: DISCORD_BOT_TOKEN
        sync: false
      - key: DISCORD_CLIENT_ID
        sync: false
      - key: DISCORD_GUILD_ID
        sync: false
      - key: DISCORD_ADMIN_ROLE_ID
        sync: false
      - key: BACKEND_URL
        sync: false
      - key: RESEND_API_KEY_MAIN
        sync: false
